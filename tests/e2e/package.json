{
  "name": "@agent-os/e2e-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "docker compose -f ../../docker-compose.yml up -d && vitest run && docker compose -f ../../docker-compose.yml down"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "vitest": "^1.6.0"
  }
}
