{
  "name": "agent-os",
  "version": "0.1.0",
  "private": true,
  "description": "Agent OS starter repo — free-tier multi-agent company operating system",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "deploy": "pnpm build && ./scripts/deploy.sh"
  },
  "devDependencies": {
    "@eslint/js": "8.57.1",
    "@types/node": "^20.14.0",
    "eslint": "8.57.1",
    "prettier": "^3.3.0",
    "tsx": "^4.15.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "8.61.1"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
